# Runtime deps for Streamlit Cloud and `streamlit run streamlit_app.py` from repo root.
# Streamlit Cloud reads this file automatically; keep it in sync with phase2/phase5/phase6 pyproject.toml.

streamlit>=1.28.0
pydantic>=2.5.0
datasets>=2.16.0,<4.0.0
huggingface_hub>=0.20.0
openai>=1.30.0
